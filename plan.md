# 棋域对战多棋种扩展计划

## Summary

将项目升级为“棋域对战”，支持五子棋、围棋、中国象棋与国际象棋的统一人机、好友房间、观战、排行榜和复盘体验。

## Implementation Changes

- 使用统一规则引擎、JSON 棋局状态和通用 REST/WebSocket 协议管理所有棋种。
- 围棋采用 19 路中国规则、7.5 贴目、位置全局同形禁入、双方停一手后双方确认死子数子。
- 国际象棋采用 `python-chess` 处理完整 FIDE 合法性与终局；中国象棋由服务端执行走子、将军与将死校验。
- 房间创建时确定棋种和时限；五子棋每步 60 秒，其他棋种为 10 分钟加 10 秒，服务端为时钟权威。
- 对局记录保存规则版本、初始局面、完整状态、棋谱、时限和终局原因；按棋种维护独立胜负统计。
- 共享页面提供棋种大厅、人机、好友房间和基础棋盘交互；已结束对局保留统一复盘 API。

## Test Plan

- 覆盖每个规则引擎的初始化、合法走子、非法走子与终局状态。
- 覆盖围棋提子、同形禁入、停一手和数子；国际象棋特殊走法；中国象棋将军与将死。
- 覆盖 REST 会话、实时房间、服务器走子校验、认输、超时和复盘读取。

## Assumptions

- 本地部署安装 `python-chess`；可选的 Stockfish、Pikafish 与 KataGo 通过环境配置接入以提升 AI 强度。
- 项目按全新多棋种架构演进，不承担旧版五子棋接口兼容成本。
